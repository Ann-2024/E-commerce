
spring:
  application:
    name: Ecommerce
  datasource:
    url: jdbc:Mysql://localhost:3306/jwt_ecommerce
    username: root
    password: Sqlserver123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG